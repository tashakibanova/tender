# Платформа для автоматизации участия в госзакупках

## ⚠️ ВАЖНО: ЗАПУСК ТОЛЬКО ЧЕРЕЗ ЛАУНЧЕР

❗ **Не запускайте `main.py` или другие скрипты напрямую.**  
❗ **Вся логика работает ТОЛЬКО через `launcher.py`.**

Если вы запустите платформу другим способом:
- поиск закупок не будет работать,
- данные организации не загрузятся,
- система не сможет определить контекст (ИНН).

## ✅ Как запустить

1. Убедитесь, что у вас установлен Python 3.9+.
2. Откройте терминал в папке проекта (там, где лежит `launcher.py`).
3. (Опционально) Создайте виртуальное окружение и активируйте его:

```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
.\.venv\Scripts\activate   # Windows PowerShell
```

4. Запустите лаунчер:

```bash
python launcher.py
```

5. В окне лаунчера:
   - Выберите или создайте организацию,
   - Настройте параметры поиска,
   - Нажмите «Запустить мониторинг».

### Что произойдет после запуска
- Создаются локальные папки в `/organizations/{ИНН}/` при первой настройке организации.
- Все данные остаются на компьютере пользователя.
- Доступ к критическим действиям блокируется до выбора организации.

### Возможные проблемы
- **Tkinter не установлен** (редко на Linux): установите пакет `python3-tk`.
- **Окно не открывается в headless-среде**: запускайте на машине с GUI.

## 📁 Структура данных

Все данные хранятся локально в папке:
```
/organizations/{ИНН}/
```

- Профиль: `profile.json` (соответствует Форме 2),
- Ключевые слова: `search_keywords.json`,
- Лоты: `/lots/`,
- Договоры: `/contracts/`.

## 🛠 Требования

- Только стандартная библиотека Python,
- Tkinter (входит в поставку),
- Никаких внешних зависимостей.
